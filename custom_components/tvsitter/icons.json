{
  "entity": {
    "binary_sensor": {
      "screen": {
        "default": "mdi:television-off",
        "state": {
          "on": "mdi:television-play"
        }
      },
      "pin_set": {
        "default": "mdi:lock-open-alert",
        "state": {
          "on": "mdi:form-textbox-password"
        }
      },
      "reporting": {
        "default": "mdi:lan-disconnect",
        "state": {
          "on": "mdi:lan-connect"
        }
      },
      "reporting_stopped": {
        "default": "mdi:check-network",
        "state": {
          "on": "mdi:alert-network"
        }
      }
    },
    "sensor": {
      "active_app": {
        "default": "mdi:apps"
      },
      "used_today": {
        "default": "mdi:timer-sand-full"
      },
      "remaining_today": {
        "default": "mdi:timer-sand"
      },
      "rules": {
        "default": "mdi:script-text"
      },
      "bonus_today": {
        "default": "mdi:timer-plus"
      },
      "limit_today": {
        "default": "mdi:timer-cog"
      },
      "last_reported": {
        "default": "mdi:clock-check"
      },
      "used_yesterday": {
        "default": "mdi:calendar-clock"
      }
    },
    "switch": {
      "lock": {
        "default": "mdi:lock-open-variant",
        "state": {
          "on": "mdi:lock"
        }
      },
      "block_settings": {
        "default": "mdi:cog",
        "state": {
          "on": "mdi:cog-off"
        }
      }
    },
    "number": {
      "daily_limit": {
        "default": "mdi:timer-cog-outline"
      },
      "warn_before": {
        "default": "mdi:bell-alert-outline"
      },
      "sleep_timer": {
        "default": "mdi:sleep"
      },
      "limit_mon": {
        "default": "mdi:calendar-today"
      },
      "limit_tue": {
        "default": "mdi:calendar-today"
      },
      "limit_wed": {
        "default": "mdi:calendar-today"
      },
      "limit_thu": {
        "default": "mdi:calendar-today"
      },
      "limit_fri": {
        "default": "mdi:calendar-today"
      },
      "limit_sat": {
        "default": "mdi:calendar-today"
      },
      "limit_sun": {
        "default": "mdi:calendar-today"
      }
    },
    "button": {
      "clear_limit": {
        "default": "mdi:timer-off-outline"
      },
      "clear_pin": {
        "default": "mdi:lock-reset"
      }
    },
    "text": {
      "parent_pin": {
        "default": "mdi:form-textbox-password"
      }
    },
    "event": {
      "time_request": {
        "default": "mdi:hand-wave"
      },
      "tamper": {
        "default": "mdi:shield-alert"
      }
    }
  },
  "services": {
    "grant_time": "mdi:timer-plus",
    "deny_time": "mdi:timer-remove",
    "set_schedule": "mdi:calendar-week",
    "set_windows": "mdi:calendar-clock",
    "set_app_limit": "mdi:timer-lock-outline",
    "use_schedule": "mdi:calendar-import",
    "forget_schedule": "mdi:calendar-remove",
    "forget_pending_rules": "mdi:playlist-remove",
    "set_allowed_apps": "mdi:playlist-check"
  }
}
