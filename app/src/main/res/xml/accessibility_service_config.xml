<?xml version="1.0" encoding="utf-8"?>
<!--
  Two capabilities are asked for, and both are kept as narrow as the platform allows.

  canRetrieveWindowContent is *not* requested. The service reads only the package and class
  name off each event, never the contents of a window, so claiming the right to read what is
  on screen — including what somebody is typing — would be privilege this app does not need.
  A parental control app that could watch a parent type their account PIN is self-defeating.

  canRequestFilterKeyEvents has to be declared here, because that is what the user consents
  to when enabling the service, and it is what makes HOME interceptable. The matching flag is
  cleared at runtime the moment the service connects, and set only while the lock screen is
  showing — see EnforcerService.setKeyFiltering. Key filtering is a keylogger capability; it
  should be live for the seconds it is needed and not a moment longer.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:summary="@string/accessibility_service_summary" />
