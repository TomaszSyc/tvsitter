# Normalise to LF in the repository and in checkouts, so a Windows contributor does not
# produce a diff of the entire tree.
* text=auto eol=lf

# Except the Windows launcher: a batch file needs CRLF.
gradlew.bat text eol=crlf

gradlew text eol=lf
*.jar binary
*.png binary
