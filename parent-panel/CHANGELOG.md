# Changelog

The version is one SemVer for the whole product (D6): the panel, the integration and the
Android app share `version.txt` and share a tag.

## 0.7.2

- The app list holds apps, rather than every package the set charged a second to.

## 0.7.1

- Syntax the container's Python actually accepts, so the panel starts.

## 0.7.0

- The panel is a control page rather than a status board: what is on, today by app, and
  every rule editable in place.

## 0.6.24

- The page actually renders: the server was importing a name that had become a method.

## 0.6.23

- The container can find its own package, so the panel actually starts.

## 0.6.22

- Built on the machine rather than pulled, so the panel installs without a published image.

## 0.6.21

- aiohttp 3.14.3, which is the first release clear of two advisories the 3.13.2 pin carried.

## 0.6.20

- The one dependency moved into `requirements.txt`, so Dependabot watches it.

## 0.6.19

- No change to the panel; the version follows the product.

## 0.6.18

- No change to the panel; the version follows the product.

## 0.6.17

- First release. One page, listing the televisions the integration can see and what each is
  doing.
